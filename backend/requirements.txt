# Web server
fastapi
uvicorn[standard]
python-multipart

# ML / CV pipeline (same stack as the original TrafficVioLens project)
# Pin torch < 2.6 so ultralytics 8.2.0 can load yolov8n.pt (torch>=2.6 defaults
# weights_only=True which breaks the checkpoint load).
torch<2.6
torchvision
ultralytics
opencv-python-headless
numpy
easyocr
pytesseract
Pillow
scikit-image
pandas
fpdf2
psycopg2-binary
